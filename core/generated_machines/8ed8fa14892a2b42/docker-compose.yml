version: '3.8'

services:
  web:
    build: .
    container_name: hackforge_8ed8fa14892a2b42
    ports:
      - "8084:80"
    volumes:
      - ./app:/var/www/html
    depends_on:
      - db
    networks:
      - hackforge_8ed8fa14892a2b42
    restart: unless-stopped

  db:
    image: mysql:8.0
    container_name: hackforge_db_8ed8fa14892a2b42
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hackforge
      MYSQL_USER: hackforge
      MYSQL_PASSWORD: hackforge123
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      - db_data:/var/lib/mysql
    networks:
      - hackforge_8ed8fa14892a2b42
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

networks:
  hackforge_8ed8fa14892a2b42:
    driver: bridge

volumes:
  db_data:
